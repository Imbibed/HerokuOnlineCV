import { WindowResizeSensitiveDirective } from './window-resize-sensitive.directive';

describe('WindowResizeSensitiveDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowResizeSensitiveDirective();
    expect(directive).toBeTruthy();
  });
});
